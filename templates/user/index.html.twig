{% extends 'base.html.twig' %}

{% block title %}Mon profil - Prête Ton Conjoint{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header">
        <h1 class="card-title">Mon profil</h1>
        <p style="color: var(--text-light); text-align: center;">Gérez vos informations personnelles</p>
    </div>

    <div style="display: grid; grid-template-columns: 300px 1fr; gap: 3rem; align-items: start;">
        <!-- Photo de profil -->
        <div style="text-align: center;">
            <div style="position: relative; margin-bottom: 2rem;">
                <img src="{{ asset('uploads/' ~ user.img) }}" alt="Photo de {{ user.pseudo }}" 
                     style="width: 100%; max-width: 250px; height: 250px; object-fit: cover; border-radius: 20px; box-shadow: 0 10px 30px var(--shadow-soft);">
            </div>
            
            <div style="background: var(--gradient-primary); color: white; padding: 0.5rem 1rem; border-radius: 20px; font-weight: 600; display: inline-block;">
                Membre actif
            </div>
        </div>

        <!-- Informations du profil -->
        <div>
            <div style="background: var(--background-light); padding: 2rem; border-radius: 20px; margin-bottom: 2rem;">
                <h2 style="color: var(--text-dark); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.5rem;">
                    👤 Informations personnelles
                </h2>
                
                <div style="display: grid; gap: 1rem;">
                    <div style="display: flex; align-items: center; padding: 1rem; background: var(--background-white); border-radius: 10px; box-shadow: 0 2px 10px var(--shadow-soft);">
                        <div style="background: var(--primary-color); color: white; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 1rem; font-weight: 600;">
                            🏷️
                        </div>
                        <div>
                            <div style="color: var(--text-light); font-size: 0.9rem;">Pseudonyme</div>
                            <div style="color: var(--text-dark); font-weight: 600; font-size: 1.1rem;">{{ user.pseudo }}</div>
                        </div>
                    </div>

                    <div style="display: flex; align-items: center; padding: 1rem; background: var(--background-white); border-radius: 10px; box-shadow: 0 2px 10px var(--shadow-soft);">
                        <div style="background: var(--secondary-color); color: white; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 1rem; font-weight: 600;">
                            📧
                        </div>
                        <div>
                            <div style="color: var(--text-light); font-size: 0.9rem;">Email</div>
                            <div style="color: var(--text-dark); font-weight: 600; font-size: 1.1rem;">{{ user.email }}</div>
                        </div>
                    </div>

                    <div style="display: flex; align-items: center; padding: 1rem; background: var(--background-white); border-radius: 10px; box-shadow: 0 2px 10px var(--shadow-soft);">
                        <div style="background: var(--accent-color); color: white; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 1rem; font-weight: 600;">
                            🎂
                        </div>
                        <div>
                            <div style="color: var(--text-light); font-size: 0.9rem;">Âge</div>
                            <div style="color: var(--text-dark); font-weight: 600; font-size: 1.1rem;">{{ user.age }} ans</div>
                        </div>
                    </div>

                    <div style="display: flex; align-items: center; padding: 1rem; background: var(--background-white); border-radius: 10px; box-shadow: 0 2px 10px var(--shadow-soft);">
                        <div style="background: var(--primary-color); color: white; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin-right: 1rem; font-weight: 600;">
                            👤
                        </div>
                        <div>
                            <div style="color: var(--text-light); font-size: 0.9rem;">Genre</div>
                            <div style="color: var(--text-dark); font-weight: 600; font-size: 1.1rem;">{{ user.sexe }}</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Actions -->
            <div style="background: var(--background-light); padding: 2rem; border-radius: 20px;">
                <h2 style="color: var(--text-dark); margin-bottom: 1.5rem; display: flex; align-items: center; gap: 0.5rem;">
                    ⚡ Actions rapides
                </h2>
                
                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 1rem;">
                    <a href="{{ path('app_conjoints_new') }}" style="text-decoration: none;">
                        <button style="width: 100%; padding: 1rem; display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
                            <span>+</span> Ajouter un produit
                        </button>
                    </a>
                    
                    <a href="{{ path('app_conjoints_index') }}" style="text-decoration: none;">
                        <button class="btn-secondary" style="width: 100%; padding: 1rem; display: flex; align-items: center; justify-content: center; gap: 0.5rem;">
                            ← Retour à la liste
                        </button>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
