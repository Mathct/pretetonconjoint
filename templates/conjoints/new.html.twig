{% extends 'base.html.twig' %}

{% block title %}Créer un nouveau conjoint- Prête Ton Conjoint{% endblock %}

{% block body %}
<div class="form-container">
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Proposer son Conjoint</h1>
            <p style="color: var(--text-light); text-align: center;">Ajoutez un nouveau conjoint à la collection</p>
        </div>

        {{ include('conjoints/_form.html.twig') }}
    </div>

    <div style="text-align: center; margin-top: 2rem;">
        <a href="{{ path('app_conjoints_index') }}">
            <button class="btn-secondary">← Retour à la liste</button>
        </a>
    </div>
</div>
{% endblock %}
